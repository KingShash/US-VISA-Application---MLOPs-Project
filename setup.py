from setuptools import setup, find_packages

setup(
    name="us_visa",
    version="0.0.0",
    author="Sunny",
    author_email="shashwat298@gmail.com",
    packages=find_packages()   # looks for constructors in file, to be cosnidered as local package
)